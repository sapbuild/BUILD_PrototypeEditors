'use strict';
// Temporary Mock services that shouldn't be required by this sample app

module.exports = angular.module('UserResearch', [])
    .factory('Studies', function () {
        return function () {}
    });
